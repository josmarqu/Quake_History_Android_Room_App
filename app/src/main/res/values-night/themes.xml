<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuakeHistory" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/soft_red</item>
        <item name="colorPrimaryDark">@color/soft_red</item>
        <item name="colorAccent">@color/soft_red</item>
        <!-- Customize your theme here. -->
    </style>
</resources>